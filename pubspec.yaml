name: todo_app_isar_db
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  isar: ^3.0.5
  isar_flutter_libs: ^3.0.5
  intl: ^0.17.0
  provider: ^6.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.2
  isar_generator: ^3.0.5

flutter:
  uses-material-design: true
